* reorganize informacion
* firewall notes: ufw, iptables, nftables, ...
* convert vim plugins to package
* vim format table: align items
* vim modify alignment, [fF] ch,
  like normal mode [fF] except use previous line(s) position.
* ydo script
  macro: keep a fullscreen topmost tk window to capture mouse/keyboard.
  Use -alpha to see through to desktop/other apps.
  On keyboard/mouse events, record.
  After a period of inactivity or a flush keybind:
    withdraw the window
    replay the flushed event sequence
      if mouse motion: ensure topmost, and some alg to move mouse to coordinates
        regardless of mouse acceleration etc.
      if mouse click or keyboard: ensure withdrawn and then click/keypress
    pop up window again.
* cppy
  cpp reference <-> py borrowed reference
  cpp value     <-> py owned reference
  clarify inheritance patterns etc...
  owned reference should be borrowed references except with extra
  cleanup (decref) on destructor...
  maybe separate constructor to create owned reference rather than
  borrowed reference?
* ipc server using generator, yield/.send()
  1. separate polling handling/(buffered) reading etc with the handling
  2. separate thread handles polling and reading/writing.
  3. io should be non-blocking.
  4. use edge polling.
  5. should read/write actions be registered together or separately?
* decorator style tkinter
  1. minimize function registration.
  2. focus more on tcl realities
  3. Tk.createcommand() creates a tcl command that calls corresponding python
     command, any arguments are passed in order, as str.
  4. maybe no bound methods? take id(self) and pass the id as the first
     (bound method's __self__ attr?)
     argument?, keep a dict of all instances...
     __del__ to remove list of instances...
