contents:
-0- general
	-0.1- terminology
-1- commands
	-1.1- Common flags/args
	-1.2- info
-2- keybindings
	-2.1- copy mode


	-0.2- configuration
-2- controls
-3- config

===========
-0- general
===========
	observations:
		typing in vim, esc to exit insert mode, then <pre> ends up in a raw <pre> being
		sent. It is not interpreted as the tmux <pre> key...

	=================
	-0.1- terminology
	=================
	Session
		A collection of windows.

	Window
		a "space" that takes the entire "screen"
		The terminal can only display a single window at a time.

	Pane
		A splitting of a "Window"

	Key sequence
		A sequence of keys that generally activate a command
		binding.  Key sequences will be separated by a space.
		A dash will indicate combination with a modifier key.
		Example: "C-b [" means press control+b and then [.

	Prefix key
		All tmux keybindings are prefixed with the "prefix key".
		In these notes, the prefix key will be omitted unless otherwise
		stated referred to as <pre>

	C-x/M-x
		C-x means press Control + the x character.
		M-x means either pressing alt + x, or pressing Esc followed by x.
		Both seem to activate the M-x bindings.

	=============
	-0.2- options
	=============
	-2
		support 256 colors
	-C
		start in control mode
	-c cmd
		start with a shell running the cmd
	-D
		do not start as a daemon
	-N
		no server
	-f altconfigfile
	-T comma-delim-client_features
	-u
		always use utf-8, ignore LC_*
	-v
		verbose logging
	-V
		version info
	-L sockname
	-S sockpath
============
-1- commands
============
Tmux implements its own scripting language which is very similar to bash.
Note that when calling tmux commands from the shell, the shell first parses
arguments, and then tmux.  This means that to chain tmux commands, the semicolon
must be escaped.

If there is no command, then new-session is used by default.
Some commands have aliases.

____________
same as bash
	Words are split by whitespace.
	The first word is a command, the rest are arguments.
	semicolon/new-line terminates a command.
	Quoting is done with '', ""
	"" allows substitutions, '' does not.
	# starts a comment
	Line continuation with trailing \

______________
diff from bash
	{} is parsed as config file and then converted to string and can be nested.
	commands separated by a ; is more like bash &&
	Line continuation is allowed everywhere except in {}

	?substitutions are only inside double quotes? double quotes are like merging fo bash "" and $'' syntax?
		Get environment variable with $env_var
		~user is replaced with their home dir, defaulting to current user
		\uXXXX \uXXXXXXXX for hex unicode
		\e \r \n \t  for esc, carriage return, newline, tab
		\ooo for octal
		\C literal C character for any beside above mentioned ones.

=======================
-1.1- Common flags/args
=======================
	====================================
	[-t|-s] [client|session|window|pane]
		These specify target client/session/window/pane for a command
		______
		client
			The pty (/dev/...) of any currently attached client.
			In the shell, `tmux list-clients` or `tty`. NOTE list-clients only shows
			currently attached client sessions.
		_______
		session
			Checked in order:
				=exactmatch
				$ID
				session-name
				start of session-name
				glob pattern
			Error if ambiguous
		______
		window
			session:window where session is as above, and window is (in order):
				=exactmatch
				special token
				window index
				window id @id
				exact window name
				window name prefix
				glob pattern
			empty indicates next unused index or current window depending on the command.
			______________
			special tokens
				long            meaning
				{start} (^)     lowest window index
				{end} ($)       highest window index
		____
		pane
			window[.id] where window is as above.  Empty id means the active pane.
			______________
			special tokens
				{top}             The top pane
				{bottom}          The bottom pane
				{left}            The leftmost pane
				{right}           The rightmost pane
				{top-left}        The top-left pane
				{top-right}       The top-right pane
				{bottom-left}     The bottom-left pane
				{bottom-right}    The bottom-right pane
				{up-of}           The pane above the active pane
				{down-of}         The pane below the active pane
				{left-of}         The pane to the left of the active pane
				{right-of}        The pane to the right of the active pane
		_____________________
		common special tokens
			{last} (!)        the previously selected item by time
			{next} (+)        the next item by index
			{previous} (-)    the previous window by index
				+- can be followed by a count
			{mouse} (=)       item with the most recent mouse activity
			{marked} (~)      marked item
		________
		item IDs
			$session
			@window
			%pane
	============
	shell-commad
		This is just a single string as would be passed to `sh -c shell-command`
		or separate arguments of a command if supported

==========
-1.2- info
==========
	lsc | list-clients
	lscm | list-commands
	ls | list-sessions
	lsk | list-keys [-1aN] [-P prefix-string -T key-table] [key]
		-T indicates the table
			copy-mode = emacs copy mode
			copy-mode-vi = vim copy mode
			prefix = normal tmux WITH <pre>
			root = bindings WITHOUT <pre>
		-N exclude copy-mode
		-P prefix before each key
		-1 only first matching key
		-a list keys without notes
	lsp | list-panes
	lsw | list-windows

	rename | rename-session [-t session] name
	renamew | rename-window

________
sessions
	attach | attach-session [-dErx] [-c working-directory] [-f flags] [-t target-session]
		creates a new client if outside tmux.  Otherwise modifies current client.
		-d: detach other clients from this session
		-x: send SIGHUP too
		-f: comma-delimited client flags
			active-pane   client's active pane does not affect other clients
			ignore-size   don't affect other clients' size
			no-output
			pause-after=seconds
			read-only
			wait-exit
		-r: alias for `-f read-only,ignore-size`
		-c: set cwd for new windows
		-E: do not use update-environment option
	new | new-session [-AdDEPX] [-c start-directory] [-e environment]
		[-f flags] [-F format] [-n window-name] [-s session-name]
		[-t group-name] [-x width] [-y height] [shell-command]
	source | source-file [-Fnqv] path ...
		Source a file.
		-F: path is a FORMAT
		-q: quiet, no errors if not exist
		-n: dry-run
		-v: verbose show commands and line no

	detach | detach-client [-aP] [-E shell-command] [-s target-session] [-t target-client]
		detach the current client.
		-t target-client
		-s target-session
		-a all clients except -t
		-P send SIGHUP
		-E run shell-command to replace client
	has | has-session -t session
	kill-server
	kill-session [-aC] [-t target-session]
	lockc | lock-client
	locks | lock-session
	refresh | refresh-client
	showmsgs | show-messages [-JT] [-t target]
	start | start-server
		server will be auto-started when creating a new session if no server.
	suspendc | suspend-client
	switchc | switch-client

_____________
windows/panes

	copy-mode
	selectl | select-layout
		even-horizontal: all panes in a horizontal line.
		even-vertical: all panes in a vertical line.
		main-horizontal: pane 0: tophalf and remaining bottom half horizontal line
		main-vertical: pane0: left half and remaining right half vertical line
		tiled: Try to make an even NxN grid
	resizep | resize-pane [-DLMRTUZ] [-t target-pane] [-x width] [-y height] [adjustment]
		-UDLR resize direction
			-x, -y: absolute x/y size
			adjustment: relative change
		-Z: zoomed
		-M: mouse resizing
		-T: trim lines
	selectp | select-pane [-DdeLlMmRUZ] [-T title] [-t target-pane]
		-DLRU: move down/left/right/up
		-l: the last pane
		-ed: enable/disable pane input
		-T: change the title
		-Z keeps it zoomed if it was
	splitw | split-window [-bdfhIvPZ] [-c start-directory] [-e environment]
			[-l size] [-t target-pane]
			[shell-command] [-F format]
		-hv: horizontal/vertical, -v by default
		-l: new pane size (lines|cols)
		-b: to left or above instead of default (right or below)
		-f: full width/height
		-Z: keep zoom
		cmd: run the command in the new pane, otherwise start a shell.
	swapp | swap-pane [-dDUZ] [-s src-pane] [-t dst-pane]
		-UD: numerical neighbor up/down
		-d: don't change active pane
		-Z: keep zoom


	breakp | break-pane
	capturep | capture-pane [-aepPqCJN] [-b buffer-name] [-E end-line] [-S start-line] [-t target-pane]
		write a pane to a buffer(-b) or stdout(-p)
	choose-client
	choose-tree [-GNrswZ] [-F format] [-f filter] [-K key-format] [-O sort-order] [-t target-pane]
	customize-mode [-NZ] [-F format] [-f filter] [-t target-pane] [template]
		Allow interactive customization of window options, keybindings, etc.
			enter     select pane/window/session etc
			up/down   select item
			+/-       expand/collapse item
			M-+/M--   expand/collapse all items
			s         set option
			S         set global option
			w         set window option
			d         set default
			D         default tagged options/keys
			u         unset option or unbind key
			U         unset/unbind tagged options/keys
			C-s       search by name
			n         repeat last search
			t         toggle tag
			T         untag all
			C-t       tag all
			f         FORMAT filter
			v         toggle option info
			q         quit
	displayp | display-panes
	findw | find-window
	joinp | join-pane
	killp | kill-pane
	killw | kill-window
	lastp | last-pane
	lastw | last-window
	linkw | link-window
	movep | move-pane
	movew | move-window
	neww | new-window
	nextl | next-layout
	next | next-window
	pipep | pipe-pane
	prevl | previous-layout
	prev | previous-window
	resizew | resize-window
	respawnp | respawn-pane
	respawnw | respawn-window
	rotatew | rotate-window
	selectw | select-window
	swapw | swap-window
	unlinkw | unlink-window





________
bindings
	bind | bind-key [-nr] [-N note] [-T key-table] key command [arguments]
		-T: bind to the given table
			prefix: the default, keys following the <pre>
			root: bindings that don't require <pre>
		-n: alias for -T root
		-r: may repeat
		-N: add a note
	unbind | unbind-key [-anq] [-T key-table] key
		-nT: same as bind
		-a: all keybindings are removed
		-q: ignore errors
	send-prefix [-2] [-t target-pane]
		Send prefix key or secondary prefix key
	send | send-keys [-FHlMRX] [-N repeat-count] [-t target-pane] key ...
		send keys, like a macro
		Uncrecognized keys are sent as a string of characters
		-l disable name lookup
		-H hex
		-R ?reset terminal state?
		-M pass through a mouse event
		-X send command into copy mode
		-N repeat count
		-F expand formats

===============
-2- keybindings
===============
C-b is the DEFAULT prefix key.
The prefix key is omitted below for convenience.

Key syntax:
	a-zA-Z0-9 itself
	C-k | ^k  control+k
	S-k       shift+k
	M-k       alt+k (though esc then k seems to work as well)
	Up
	Down
	Left
	Right
	BSpace
	BTab
	DC        delete
	End
	Enter
	Escape
	F[1-12]
	Home
	IC        insert
	NPage|PageDown|PgDn
	PPage|PageUp|PgUp
	Space
	Tab
	Any

Use tmux list-keys to list all key bindings.

_______
general
	C-b         Send the prefix key (C-b) through to the application.
	?           List all key bindings.
	d           Detach the current client.
	D           Choose a client to detach.
	:           Enter the tmux command prompt.
	f           Prompt to search for text in open windows. (doesn't seem to search, just does same as w)
	r           Force redraw of the attached client.
	t           Show the time.
	~           Show previous messages from tmux, if any.
	C-z         Suspend the tmux client.

__________
copy/paste
	[           Enter copy mode to copy text or view the history.
	]           Paste the most recent tmux-copied buffer.
	#           List all paste buffers. (All items that have been copied in copy mode.)
	-           Delete the most recently copied buffer of text.
	=           Choose which buffer to paste interactively from a list.
	Page Up     Enter copy mode and scroll one page up.

	copy/paste mode is how you would scroll up the buffer.
	Note that copy/paste mode copies to clipboard so you can paste to other
	applications etc.
	While in copy-paste mode, depending on "${EDITOR}" environment variable,
	the controls might be in emacs mode or vim mode.  Otherwise, it
	defaults to emacs.

	_____
	emacs
		C-space   start selection
		C-g       clear selection
		C-w       copy, (emacs is yank but doesn't actually yank)
		M-w       copy

		C-f, C-b  move forward/backward 1 char
		M-f, M-b  move forward/backward 1 word
		C-a, C-e  move to front/back
		C-n, C-p  move to next/previous line

		escape    cancel
		q         cancel

		g         go to line (0 is bottom, matches the n/N at topright)
		C-v       scroll down a page
		M-v       scroll up a page

	___
	vim
		space   set mark
		v       rectangle toggle
		escape  clear selection
		return  copy

		q       cancel

		hjkl    move 1 char left down up right
		bw      move back/forward 1 word
		C-[u|d] up/down half-page
		0$      move to front/back
		^       first non-blank
		HML     top/mid/bottom of screen
		g|G     top/bottom of buffer

______________
windows (tabs)
	c     Create a new window.
	&     Kill the current window.

	0-9   Move to window N
	l     Move to the previously selected window.
	n     Move to the next window.
	p     Move to the previous window.
	w     Choose the current window interactively.
	'     Prompt for a window index to select.
	.     Prompt for an index to move the current window.
	M-n   Move to the next window with a bell or activity marker.
	M-p   Move to the previous window with a bell or activity marker.

	i     Display some information about the current window.

_____
panes
	"             Split the current pane into two, top and bottom.
	%             Split the current pane into two, left and right.

	;             Move to the previously active pane.
	o             Move to next pane.
	arrowkeys     Change to the adjacent pane in the chosen direction.

	E             equalize windows
	C-arrowkeys   Resize the current pane in steps of 1 cell.
	M-arrowkeys   Resize the current pane in steps of 5 cells.
	z             Toggle zoom state of the current pane. (moving panes toggles off zoom.)

	C-o           Rotate the panes in the current window forwards.
	M-o           Rotate the panes in the current window backwards.
	{             Swap the current pane with the previous pane.
	}             Swap the current pane with the next pane.
	M-1 to M-5    Select the preset layout (see select-layout above)
	Space         Arrange panes in the next preset layout.

	M             Clear the marked pane.
	m             Mark the current pane (see select-pane -m).

	!             Move current pane to a new window.
	q             Briefly display pane indexes. (but no binding to select by index?)
	x             Kill the current pane.

________
sessions
	(           Switch the attached client to the previous session.
	)           Switch the attached client to the next session.
	L           Switch the attached client back to the last session.
	s           Select a new session for the attached client interactively.

________
renaming
	$     Rename the current session.
	,     Rename the current window.


==========
-3- config
==========

name=value          set env var
%hidden name=value  local tmux var
%if format
%elif format
%else
%endif


The config file is ".tmux.conf" in the user's home directory.

unbind keycombo         unbinds a binding.
bind keycombo command   bind a command to a keycombo
set [flags] key value   set a key/value setting
	flags:
		-s                  server setting (or use set)
		-w                  window setting (or use setw)
		-g                  global option

useful commands:
	unbind C-b              remove C-b binding so terminal can go back
	set -g prefix C-q       rebind to C-q because unused
	setw -g mode-keys vi    use vi copy mode
