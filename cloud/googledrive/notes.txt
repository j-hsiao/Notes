https://developers.google.com/workspace/drive/api/guides/manage-uploads

-0- resources
-1- authorization
-2- upload
-2.0- create
-2.1- simple
-2.2- multipart
-2.3- resumable
-3- download

=============
-0- resources
=============
The base uri for resources is:
https://www.googleapis.com/drive/v[2|3]

shortcuts must have a MIME: application/vnd.google-apps.shortcut
Files should be given a `name` with extension, but when getting the file,
the extension is in a separate `fileExtension` property

=================
-1- authorization
=================
Authentication:
	Determine who is accessing apis.
	user
	app (via settings and credentials)

authorization:
	permission to do some action

credential
	a cert, user/password, etc

scope:
	? do they all start with https://www.googleapis.com/auth?
	workspace app name, data, permissions
	example:
		https://www.googleapis.com/auth/drive.appdata


There are 3 types of authorization:
api key
	access publicly available data
	https://console.cloud.google.com/apis/credentials and create an api key
	these only work for public data (ex. shared with everone)
oauth client id
	access user data
	create a "client": a program that uses apis on behalf of client.
		web application
			register redirect urls
			recommend save json.
			"Note that client secrets aren't used for web applications"
		desktop
			save secret etc.
service account
	access ?app-private? data on behalf of users

-----
Steps
-----
1. create/register app with google cloud console and define required scopes etc.
	Go to https://console.developers.google.com/auth/branding
	create a project
		if no organization must select external users
		* app name
		* user support email
		* audience
		* contact info
		* if external:
			- add a test user
	Add scopes:
		Data Access -> add/remove scopes
		https://www.googleapis.com/auth/drive[[.appdata|.file|.photos.readonly]]

2. authenticate
3. request resources
4. ask for user consent
5. send approved request
6. get access token
7. access resources
8. refresh if needed



==========
-2- upload
==========
============
-2.0- create
============

metadata-only:
	POST https://www.googleapis.com/drive/v3/files
with file:
	POST https://www.googleapis.com/upload/drive/v3/files

required scopes:
	https://www.googleapis.com/auth/drive[[.appdata|.file]]

============
-2.1- simple
============
small file (5mb)
===============
-2.2- multipart
===============
small file (5mb) + metadata


===============
-2.3- resumable
===============
___________________________________________________________________
1. Send the initial request and retrieve the resumable session URI.

	POST https://www.googleapis.com/upload/drive/v3/files?uploadType=resumable

	headers:
		_____________________
		X-Upload-Content-Type
			Optional. Set to the MIME type of the file data, which is transferred in
			subsequent requests. If the MIME type of the data is not specified in the
			metadata or through this header, the object is served as
			application/octet-stream.
		_______________________
		X-Upload-Content-Length
			Optional. Set to the number of bytes of file data, which is transferred in
			subsequent requests.
		____________
		Content-Type
			Required if you have metadata for the file. Set to
			application/json; charset=UTF-8.
		______________
		Content-Length
			Required unless you use chunked transfer encoding. Set to the number of
			bytes in the body of this initial request.

	response has a "Location" header
	example:
		HTTP/1.1 200 OK
		Location: https://www.googleapis.com/upload/drive/v3/files?uploadType=resumable&upload_id=xa298sd_sdlkj2
		Content-Length: 0

	metadata goes in the request body, if any.
____________________________________________
2. Upload the data and monitor upload state.
	PUT to the Location value (the resumable session uri)
	set content-length, file contents in body.
____________________________________________________________
3. (optional) If the upload is disturbed, resume the upload.
	a. get status:
		PUT Locationuri
		Content-Range: */totalbytes or */*
		content-length: 0

		response:
		200 OK / 201 Created -> completed
		404 not found -> session expired
		308 resume incomplete -> must continue
			Check the Range header for bytes received.
			If absent, assume 0.
			example:
				bytes=0-42
			note: byte range is 0-indexed and inclusive on both ends.

	b. send data
		PUT ...
		Content-Range: bytes start-stop

		send bytes, 0-indexed

============
-3- download
============
